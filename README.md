# 📱 Hora Scanner - Web Application

Веб-приложение для распознавания номеров הוראה (номер инструкции) машин в клинруме с помощью ИИ.

## ✨ Возможности

- 📸 Загрузка фото номера הוראה (drag & drop или выбор файла)
- 🤖 Автоматическое распознавание номера через GPT-4o Vision
- 💾 Сохранение всех номеров в базе данных SQLite
- 📊 Просмотр списка всех добавленных машин
- 🗑️ Удаление записей
- 📥 Экспорт в CSV для Excel/Google Sheets
- 🌐 Полная веб-версия (работает на любом устройстве)
- 🇮🇱 Поддержка иврита

## 🚀 Установка

### 1. Установите зависимости

```bash
cd /Users/valerysandler/script
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Убедитесь, что `.env` имеет API ключ

```bash
cat .env
```

Должно быть:
```
OPENAI_API_KEY=sk-proj-ваш-ключ-здесь
```

### 3. Создайте необходимые папки

```bash
mkdir -p /Users/valerysandler/script/uploads
```

## 🏃 Запуск

```bash
source venv/bin/activate
python3 app.py
```

Вы должны увидеть:
```
 * Running on http://0.0.0.0:5000
```

Откройте в браузере: **http://localhost:5000**

## 📱 Использование

### На компьютере:
1. Откройте http://localhost:5000
2. Перетащите фото номера הוראה в область загрузки или нажмите на неё
3. Подождите, пока ИИ распознает номер
4. Номер автоматически добавится в базу данных

### На телефоне (в той же сети):
1. Узнайте IP адрес компьютера:
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```
2. Откройте в браузере телефона: `http://IP_ВАШЕГО_КОМПЬЮТЕРА:5000`
3. Используйте камеру телефона для фото

## 📊 База данных

Все данные хранятся в:
```
/Users/valerysandler/script/output/machines.db
```

Экспортированные CSV файлы:
```
/Users/valerysandler/script/output/machines_YYYYMMDD_HHMMSS.csv
```

Загруженные фото:
```
/Users/valerysandler/script/uploads/
```

## 🔧 API Endpoints

- `GET /` - главная страница
- `POST /api/upload` - загрузить и обработать фото
- `GET /api/machines` - получить все машины
- `DELETE /api/machines/<id>` - удалить машину
- `GET /api/export` - экспортировать в CSV

## 🆘 Решение проблем

### Ошибка "OPENAI_API_KEY не установлена"
Убедитесь, что в файле `.env` есть корректный API ключ:
```bash
nano .env
```

### Ошибка при доступе с телефона
Убедитесь, что компьютер и телефон в одной сети (WiFi).

### Медленное распознавание
Первый запрос может быть медленным (до 30 сек). Это нормально.

## 📝 Примечания

- Максимальный размер фото: 16MB
- Поддерживаемые форматы: JPG, PNG, GIF, WebP
- Номер הוראה должен быть четко виден на фото
- Все номера хранятся уникально (дубликаты не добавляются)

## 🔒 Безопасность

- Все API ключи хранятся в `.env` (не коммитится)
- Фото сохраняются локально
- База данных локальная SQLite

## 📞 Контакт для вопросов

Если возникнут проблемы, проверьте:
1. Установлены ли все зависимости: `pip list`
2. Активирована ли виртуальная среда
3. Работает ли API ключ OpenAI
4. Открыт ли порт 5000

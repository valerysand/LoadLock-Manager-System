# 🚀 Развертывание LoadLock Manager

## Вариант 1: Локальный запуск

### Быстрый старт
```bash
cd /Users/valerysandler/script
source venv/bin/activate
python3 app.py
```

**Доступно по:**
- 🖥️ Локально: http://localhost:5001
- 📱 В сети: http://IP_ВАШЕГО_КОМПЬЮТЕРА:5001

### Узнать IP компьютера:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

---

## Вариант 2: Docker

### Необходимые требования:
- Docker установлен на компьютере

### Шаги развертывания:

1. **Соберите образ**:
```bash
cd /Users/valerysandler/script
docker build -t loadlock-manager .
```

2. **Запустите контейнер**:
```bash
docker run -p 5001:5001 \
  -e OPENAI_API_KEY=sk-proj-your-key-here \
  -v $(pwd)/output:/app/output \
  -v $(pwd)/uploads:/app/uploads \
  loadlock-manager
```

3. **Откройте браузер**: http://localhost:5001

### Сохранение данных между перезапусками:
Флаги `-v` сохраняют данные на хосте в папках `output/` и `uploads/`

---

## Вариант 3: Облако (Heroku)

### Шаги развертывания:

1. **Установите Heroku CLI**:
```bash
brew install heroku/brew/heroku
heroku login
```

2. **Создайте файл `Procfile`** (если его нет):
```bash
echo "web: python3 app.py" > Procfile
```

3. **Обновите port в app.py**:
```python
if __name__ == '__main__':
    port = int(os.environ.get('PORT', 5001))
    app.run(debug=False, host='0.0.0.0', port=port)
```

4. **Инициализируйте Git**:
```bash
git init
git add .
git commit -m "Initial commit"
```

5. **Создайте приложение**:
```bash
heroku create your-loadlock-app
```

6. **Установите переменные**:
```bash
heroku config:set OPENAI_API_KEY=sk-proj-your-key-here
```

7. **Задеплойте**:
```bash
git push heroku main
```

**Приложение доступно по**: https://your-loadlock-app.herokuapp.com

---

## Вариант 4: AWS/Azure/Google Cloud

### AWS EC2:
```bash
# 1. Создайте EC2 экземпляр (Ubuntu 22.04)
# 2. SSH в экземпляр
# 3. Установите Python и зависимости
sudo apt update
sudo apt install python3-pip python3-venv git

# 4. Клонируйте репозиторий или скопируйте файлы
# 5. Установите зависимости
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 6. Запустите приложение
python3 app.py
```

---

## 📋 Требования для всех вариантов:

✅ **OpenAI API ключ** - необходим для работы распознавания  
✅ **Интернет соединение** - для связи с OpenAI API  
✅ **Достаточно места** - для сохранения фото и БД  

---

## 🔒 Безопасность

### Перед продакшеном:

1. **Отключите DEBUG режим** в `app.py`:
```python
app.run(debug=False, host='0.0.0.0', port=5001)
```

2. **Используйте SSL/HTTPS** (настройте NGINX/Cloudflare)

3. **Защитите приложение паролем** - добавьте авторизацию:
```python
from flask_httpauth import HTTPBasicAuth
auth = HTTPBasicAuth()

@auth.verify_password
def verify_password(username, password):
    return username == 'admin' and password == 'your_password'

@app.route('/api/loadlocks', methods=['GET'])
@auth.login_required
def get_loadlocks():
    # ...
```

4. **Ограничьте доступ по IP** через файрвол

5. **Сохраняйте резервные копии БД** регулярно

---

## 📊 Мониторинг

### Логирование:
```bash
# Локально
tail -f output/app.log

# На Heroku
heroku logs --tail
```

### Проверка статуса:
```bash
curl http://localhost:5001/api/loadlocks
```

---

## 🆘 Решение проблем

### Приложение не запускается
```bash
# Проверьте, активирована ли виртуальная среда
source venv/bin/activate

# Проверьте установку зависимостей
pip list

# Проверьте .env файл
cat .env
```

### Ошибка подключения к API
```bash
# Проверьте API ключ
echo $OPENAI_API_KEY

# Проверьте интернет соединение
curl https://api.openai.com
```

### Проблема с портом
```bash
# Занятый порт - используйте другой
python3 app.py --port 5002

# Или в коде измените port=5002
```

---

## 💡 Рекомендуемая конфигурация для продакшена:

```yaml
Production Setup:
  - Docker контейнер с приложением
  - PostgreSQL база данных (вместо SQLite)
  - Redis для кэширования
  - NGINX как reverse proxy
  - SSL сертификат (Let's Encrypt)
  - CloudFlare для DDoS защиты
  - Backup на облаке (AWS S3, Google Cloud Storage)
```

---

**Выбирайте вариант в зависимости от ваших потребностей!** 🚀
